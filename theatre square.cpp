#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;

int main()
{
	long long n,m,a;
	cin >> n >> m >> a;
	cout<< setprecision(19) << max(double(1) , ceil(double(n)/a)) * max(double(1), ceil(double(m)/a))<<endl;
:close

:
:q


:ls
fdf
quit
